# ==== Core Framework ====
Flask==2.3.3                  # Lightweight WSGI web framework

# ==== Production Server ====
gunicorn==21.2.0              # WSGI HTTP server for production (used by Render)

# ==== Environment Configuration ====
python-dotenv==1.0.1          # Load environment variables from .env file

# ==== CORS (for API access from front-end apps) ====
Flask-Cors==4.0.0             # Cross-Origin Resource Sharing

# ==== HTTP Requests ====
requests==2.31.0              # Makes HTTP calls (optional)

# ==== Database (PostgreSQL) ====
SQLAlchemy==2.0.30            # SQL ORM
Flask-SQLAlchemy==3.1.1       # Flask integration with SQLAlchemy
psycopg2-binary==2.9.9        # PostgreSQL driver (used with Render PostgreSQL)

# ==== Optional: ML and Image Processing Support ====
torch==2.2.2                  # PyTorch (if using machine learning)
torchvision==0.17.2           # PyTorch vision utilities
scikit-image==0.22.0          # Image processing

# ==== Optional: Data Manipulation ====
pandas==2.2.2                 # Data manipulation and analysis
